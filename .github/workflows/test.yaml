---
name: Checks

on: [ push, pull_request ]
jobs:
  rtc:
    runs-on: ubuntu-latest
    name: RuboCop TODO
    steps:
      - uses: luchihoratiu/rtc-action@test
        env:
          RTC_TOKEN: ${{ secrets.RTC_TOKEN }}
          UPDATE_PR: true
